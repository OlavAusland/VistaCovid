export type StackParameters = {
    Register : object,
    Room : {roomId: string},
    Profile : object,
    Login : object,
    VistaCovid : object
}

export type TabParameters = {
    Profile : object,
    Home : object,
    Room: {roomId: string},
    Register : object
}